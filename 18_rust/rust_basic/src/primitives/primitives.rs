pub mod primitives {
    pub fn sample_primitives() {
        let logical: bool = true;

        let a_float: f64 = 1.0;
        let an_integer = 5i32;

        let mut mutable = 12;
        // mutable = true; -> Error

        let mutable = true;
    }
}

